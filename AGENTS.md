# Code Style & Comments Rule

Never add comments to code unless directly instructed. If you think a comment is valuable, ask the user for confirmation first.

# Git Operations Rule

Do not push to remote repository branches unless explicitly asked by the user, but commit local changes frequently.
